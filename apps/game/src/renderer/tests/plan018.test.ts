import { describe, it } from 'vitest';

describe('Renderer tileAssets mapping (Plan018)', () => {
    it.todo(
        'Tests for map rendering and tile asset mapping should be moved to the @proj-tower/maprender package.'
    );
});
