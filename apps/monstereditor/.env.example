# Monster Editor agent environment variables

# Required Gemini configuration
GEMINI_API_KEY=your-gemini-api-key
GEMINI_MODEL=models/gemini-1.5-flash

# Optional outbound proxy configuration (leave empty if unused)
HTTP_PROXY=
HTTPS_PROXY=

# Optional server host/port overrides
# HOST=0.0.0.0
# PORT=5173
DOUBAO_GRPC_URL=localhost:50052