# Project-wide environment defaults for local development

# Required for Monster Editor agent (Gemini)
GEMINI_API_KEY=your-gemini-api-key
GEMINI_MODEL=models/gemini-1.5-flash

# Optional outbound proxy configuration (leave empty if unused)
HTTP_PROXY=
HTTPS_PROXY=

# Optional server host/port overrides
# HOST=0.0.0.0
# PORT=5173
